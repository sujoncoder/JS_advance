// The for statement creates a loop with 3 optional expressions:
const cars = ["toyota", "royal", "bmw", "ss"];
for (let i = 0; i < cars.length; i++) {
    const element = cars[i];
    console.log(element);

}